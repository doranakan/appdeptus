import { type BottomSheetModalMethods } from '@gorhom/bottom-sheet/lib/typescript/types'
import { createRef } from 'react'

const unitBottomSheetRef = createRef<BottomSheetModalMethods>()

export default unitBottomSheetRef
