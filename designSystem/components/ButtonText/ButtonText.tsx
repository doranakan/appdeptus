import { styled } from '@gluestack-style/react'
import { ButtonText as GSButtonText } from '@gluestack-ui/themed'

const ButtonText = styled(GSButtonText, {
  fontFamily: 'NotoSerif'
})

export default ButtonText
